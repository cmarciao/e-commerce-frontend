import styled from "styled-components";

export const ButtonContainer = styled.button`
    padding: 0.875rem 2rem;
    border-radius: 10px;

    background: white;
    color: white;
    font-weight: bold;
    text-transform: uppercase;

    user-select: none;

    background: rgb(180, 39, 184);
    background: linear-gradient(
        90deg,
        rgba(180, 39, 184, 1) 5%,
        rgba(232, 154, 35, 1) 100%
    );
`;
